# 代码提交

我的当前分支(869)都是 v5x.x.x 的形式，例如 v50.8.7，表示的是 v5 0.8.7 的版本。
我的tag会自动触发 GitHub 的构建脚本，我叫你推送的时候最后的一个版本加上 tag 推送。

# 数据库

当前环境变量的数据库是我家里的，私有的一般连不上，测试的时候不用等数据库链接。

# 环境

我当前的 ruby 使用 mise 环境的，不用使用当前系统的 ruby

