<div>
  登录表单
</div>
<%= form_with url: login_path, method: :post do |form| %>
  <label for="protocol">选择 微信协议</label>
  <%= form.select :protocol, options_for_select(@protocol_options), { include_blank: '请选择' }, { required: true } %>
  <%= form.submit "确定" %>
<% end %>